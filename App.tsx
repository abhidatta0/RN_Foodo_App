import React from 'react';
import {
  SafeAreaView,
} from 'react-native';
import AllMenu from './src/pages/AllMenu';

const App = () => {
  return (
    <SafeAreaView>
      <AllMenu />
    </SafeAreaView>
  );
};

export default App;